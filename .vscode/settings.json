{
    "runOnSave.commands": [
        // export codelab on save
        {
            "globMatch": "src/**/*.md",
            "command": "claatw.sh ${file}",
            "runIn": "backend"
        }
    ]
}